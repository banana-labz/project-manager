import React from "react"

import { Typography } from "@mui/material"

import { Layout } from "../components/Layout"

export const ErrorPage = () => (
  <Layout>
    <Typography>
      You have some kind of error, you see...
    </Typography>
  </Layout>
)

export default ErrorPage